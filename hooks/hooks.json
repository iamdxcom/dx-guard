{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scan.sh\"",
            "timeout": 15,
            "statusMessage": "🔍 dx-guard: scanning dependencies..."
          }
        ]
      }
    ]
  }
}
